spring:
  profiles:
    active: ${PROFILE_ACTIVE:dev}
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8888}
      username: ${CONFIG_USERNAME:LeviPorto}
      password: ${CONFIG_PASSWORD:q11W22e33}